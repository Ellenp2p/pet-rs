[package]
name = "wasm_channel_telegram"
version = "0.1.0"
edition = "2021"
description = "Telegram Bot API channel plugin for agent-pet-rs (WASM)"

[lib]
crate-type = ["cdylib"]

[profile.release]
panic = "abort"
lto = true
opt-level = 'z'
